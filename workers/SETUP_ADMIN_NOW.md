# Настройка Admin Dashboard - Пошагово

## ✅ Шаг 1: Установка ADMIN_API_KEY

Выполните эту команду:

```powershell
cd workers
wrangler secret put ADMIN_API_KEY
```

**Что произойдет:**
1. Wrangler попросит ввести ключ
2. Введите любой безопасный ключ (например: `admin-secret-key-2024`)
3. Нажмите Enter дважды (или Ctrl+Z и Enter для завершения)
4. Ключ будет сохранен как секрет в Cloudflare

**Генерация случайного ключа (опционально):**
```powershell
-join ((65..90) + (97..122) + (48..57) | Get-Random -Count 32 | ForEach-Object {[char]$_})
```

## ✅ Шаг 2: Развертывание Backend

После установки ключа, разверните worker:

```powershell
cd workers
wrangler deploy
```

Это обновит ваш worker с новыми админ-роутами.

## ✅ Шаг 3: Запуск Frontend локально

Откройте новый терминал и выполните:

```powershell
cd workers\admin

# Вариант 1: Python
python -m http.server 8000

# Вариант 2: Node.js (если установлен serve)
npx serve .

# Вариант 3: PHP
php -S localhost:8000
```

## ✅ Шаг 4: Использование

1. Откройте браузер: `http://localhost:8000`
2. Вы увидите поле "Enter Admin API Key"
3. Введите тот же ключ, который вы установили в шаге 1
4. Нажмите "Connect"
5. API ключ сохранится в localStorage
6. Статистика загрузится автоматически!

## 🔍 Проверка установки

Проверить что ключ установлен:

```powershell
cd workers
wrangler secret list
```

Должен появиться `ADMIN_API_KEY` в списке секретов.

## ⚠️ Важные замечания

1. **API ключ** хранится в localStorage браузера - при очистке кеша нужно будет ввести заново
2. **API URL** уже настроен в `api.js` на ваш worker: `https://rsi-workers.vovan4ikukraine.workers.dev`
3. **CORS** уже настроен на backend для работы с любыми origin

## 🚀 Production

Для production размещения используйте Cloudflare Pages:
1. Cloudflare Dashboard → Pages
2. Create a project → Upload assets
3. Загрузите папку `workers/admin/`
4. Deploy




