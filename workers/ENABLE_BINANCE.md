# Как включить Binance обратно

## 🔧 Включение Binance

Когда прокси будет готов и настроен:

### Шаг 1: Настроить прокси на VPS
Следуйте инструкции в `workers/proxy/DEPLOY.md`

### Шаг 2: Установить BINANCE_PROXY_URL
```bash
cd workers
wrangler secret put BINANCE_PROXY_URL
# Ввести URL прокси: https://your-vps-domain.com
```

### Шаг 3: Включить Binance в коде

Откройте `workers/src/data-provider-service.ts` и измените:

```typescript
// Найти эту строку (около строки 36):
private static readonly USE_BINANCE = false;

// Изменить на:
private static readonly USE_BINANCE = true;
```

### Шаг 4: Передеплоить
```bash
wrangler deploy
```

### Шаг 5: Проверить логи

В Cloudflare Dashboard должны видеть:
```
Requesting Binance candles for BTCUSDT 15m via proxy: https://your-proxy.com/api/v3/klines...
```

## ✅ Готово!

После этого система будет:
1. Пробовать Binance через прокси для криптовалют
2. При ошибке прокси → автоматический fallback на Yahoo
3. Для не-криптовалют → всегда Yahoo

---

**Текущее состояние**: Binance отключен (`USE_BINANCE = false`), используется только Yahoo.
