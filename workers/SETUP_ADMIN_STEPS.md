# Пошаговая настройка Admin Dashboard

## ✅ Шаг 1: Установка ADMIN_API_KEY

Выполните в терминале:

```powershell
cd workers
wrangler secret put ADMIN_API_KEY
```

**Что делать:**
1. Команда запросит ввод ключа
2. Введите надежный ключ (например, сгенерируйте случайную строку)
3. Нажмите Enter
4. Ключ будет сохранен как секрет в Cloudflare

**Пример ключа:** `admin-secret-key-2024-change-me-in-production`

## ✅ Шаг 2: Развертывание Backend

Backend уже включен в ваш worker. Просто разверните:

```powershell
cd workers
wrangler deploy
```

Это обновит ваш worker с новыми админ-роутами.

## ✅ Шаг 3: Настройка Frontend (локально для тестирования)

### 3.1. Обновить API URL (если нужно)

Файл `workers/admin/js/api.js` уже настроен использовать текущий origin. 

**Для локального тестирования:** раскомментируйте и измените строку в `api.js`:
```javascript
const API_BASE_URL = 'https://rsi-workers.vovan4ikukraine.workers.dev';
```

### 3.2. Запустить локальный сервер

В PowerShell:

```powershell
cd workers/admin

# Вариант 1: Python (если установлен)
python -m http.server 8000

# Вариант 2: Node.js serve
npx serve .

# Вариант 3: PHP (если установлен)
php -S localhost:8000
```

### 3.3. Открыть в браузере

Откройте: `http://localhost:8000`

## ✅ Шаг 4: Использование

1. **Откройте админ-панель** в браузере
2. **Введите API ключ** (который вы установили в шаге 1)
3. **Нажмите "Connect"**
4. API ключ сохранится в localStorage
5. **Вы увидите статистику** вашего приложения!

## 🔍 Проверка

Проверить что API ключ установлен:

```powershell
cd workers
wrangler secret list
```

Должен появиться `ADMIN_API_KEY` в списке.

## 🚀 Production (Cloudflare Pages)

Для production размещения:

1. Перейдите в [Cloudflare Dashboard → Pages](https://dash.cloudflare.com/)
2. Create a project → Upload assets
3. Загрузите папку `workers/admin/`
4. Deploy

Админ-панель будет доступна по адресу: `your-project.pages.dev`

## ❓ Troubleshooting

**Ошибка "Unauthorized":**
- Проверьте что API ключ правильно введен
- Убедитесь что ключ установлен: `wrangler secret list`
- Проверьте что worker развернут: `wrangler deploy`

**Ошибка "Failed to fetch":**
- Проверьте что API_BASE_URL правильный в `api.js`
- Проверьте CORS настройки (должны работать, так как origin: '*' уже настроен)

**Статистика не загружается:**
- Проверьте что worker развернут с последними изменениями
- Проверьте логи worker'а в Cloudflare Dashboard




